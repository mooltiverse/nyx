export const project = {
    // Project Version.
    // This variable can be used as {project.version} to parametrize the project version
    // once you have imported the file like:
    //      import {project} from '@site/src/data/projectVariables';
    // ATTENTION: what you see here is a default value that is automatically updated by
    // the build process, do not edit manually. If you see this value returned by
    // {project.version} it means you need to run the build script to update it (using Nyx).
    version: '0.1.0'
};
